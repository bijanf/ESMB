# Chronos-ESM Dependencies

# Core JAX stack
jax>=0.4.0
jaxlib>=0.4.0

# Multi-level atmospheric dynamical core (Google Research's differentiable JAX
# spectral primitive-equation dycore, the engine behind NeuralGCM). Used to
# replace the single-level barotropic atmosphere with a baroclinic multi-level
# one. 1.2.1 is compatible with jax 0.8.1 (no jax upgrade needed). NOTE: do NOT
# use `jcm` -- jcm 1.1.1 on PyPI is built against an unreleased dinosaur and is
# broken; we build on dinosaur directly. See experiments/dino_held_suarez.py.
dinosaur-dycore==1.2.1

# Numerical computing
numpy>=1.24.0

# Testing and reference (SciPy for verification only)
scipy>=1.10.0
pytest<8.0.0
xarray
netCDF4
pooch==1.8.2

# Optional: for future visualization
matplotlib>=3.7.0

# Validation framework (obs/reanalysis benchmarking)
cdsapi>=0.7.0   # ERA5 download via Copernicus CDS (needs ~/.cdsapirc)

# Development & Code Quality
black>=23.3.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.3.0
pre-commit>=3.3.0
types-requests
